# Example environment variables for RL Arena Executor

# Server configuration
EXECUTOR_HOST=0.0.0.0
EXECUTOR_PORT=50051

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# Configuration file path (optional)
# CONFIG_PATH=/path/to/limits.yaml

# Docker settings (optional overrides)
# USE_DOCKER=true
# DOCKER_IMAGE=python:3.10-slim

# Resource limits (optional overrides)
# CPU_COUNT=1
# MEMORY_LIMIT=512m
# MATCH_TIMEOUT_SEC=300
# STEP_TIMEOUT_SEC=5

# Paths (optional overrides)
# TMP_DIR=/tmp/agent_code
# REPLAY_DIR=/tmp/replays
